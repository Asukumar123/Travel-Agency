/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://voyageavecprince.com", // replace with your domain
  generateRobotsTxt: true, // will also create robots.txt
  sitemapSize: 7000,       // splits files if you have many pages
}
